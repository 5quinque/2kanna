{% extends 'base.html.twig' %}

{% block title %}Post
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <a class="btn btn-sm btn-dark float-right" href="{{ path('board_show', {'name': post.board.name}) }}">{{ post.board.name }}</a>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-4 m-auto">
            {% set button_label='Reply' %}
            {{ include('post/_form.html.twig') }}
        </div>
    </div>
    <hr>
    <div class="row">
        <div class="col-12">
            <div class="card border-primary mb-3">
                <div class="card-header">
                    {{ post.title }}
                    {% if is_granted('ROLE_ADMIN') %}
                    <span class="float-right">
                        <a href="{{ path('post_edit', {'id': post.id}) }}">edit</a>
                        {{ include('post/_delete_form.html.twig') }}
                    </span>
                    {% endif %}
                </div>
                <div id="{{ post.id }}">
                    <div class="card-body">
                        <sub class="float-right text-muted">{{ post.created|date("Y-m-d H:i:s") }}</sub>
                        <p class="card-text">
                            {{ post.message|nl2br|gt2quote }}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    {% include "post/_tree.html.twig" %}
{% endblock %}
