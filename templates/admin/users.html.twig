{% extends 'admin/index.html.twig' %}

{% block title %}Admin Dashboard - Users
{% endblock %}

{% block adminsection %}
    <div class="row">
        <div class="col-6">
            {{ include('admin/_adminuser_form.html.twig') }}
        </div>
        <div class="col-6">
            <ul class="list-group">
                {% for user in users %}
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        {{ user.username }}
                        {# {{ include('admin/_removebadword_form.html.twig') }} #}
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}
