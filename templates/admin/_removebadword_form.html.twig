<form class="d-inline" method="post" action="{{ path('admin_removefilter', {'id': word.id}) }}" onsubmit="return confirm('Are you sure you want to remove filter \'{{ word.badWord }}\'?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ word.id) }}">
    <button type="submit" class="btn btn-primary">Remove Filter</a>
</form>