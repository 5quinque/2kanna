<form class="d-inline" method="post" action="{{ path('admin_unban', {'id': banned.id}) }}" onsubmit="return confirm('Are you sure you want to unban \'{{ banned.ipAddress }}\'?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ banned.id) }}">
    <button type="submit" class="btn btn-primary">unban</a>
</form>