{% extends 'admin/index.html.twig' %}

{% block title %}Admin Dashboard - User Edit
{% endblock %}

{% block adminsection %}
    <div class="row">
        <div class="col-50 m-auto">
            {% set button_label = "Save" %}
            {{ include('admin/users/_edit_form.html.twig') }}
        </div>
    </div>
    <div class="row">
        <a href="{{ path('admin_users_password', {'username': app.request.get('username')}) }}">Change Password</a>
    </div>
{% endblock %}
