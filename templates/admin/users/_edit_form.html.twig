{{ form_start(user_form) }}
{{ form_errors(user_form) }}
<div class="form-group">
    <div class="form-row">
        {{ form_errors(user_form.username) }}
        <div class="col">
            <div class="input-group">
                {{ form_widget(user_form.username) }}
            </div>
        </div>
    </div>
</div>
<div class="form-group">
    <div class="form-row">
        {{ form_errors(user_form.roles) }}
        <div class="col">
            <div class="input-group">
                {{ form_widget(user_form.roles) }}
            </div>
        </div>
    </div>
</div>
<div class="input-group">
    <button class="btn btn-primary">{{ button_label|default('Add User') }}</button>
</div>

{{ form_end(user_form) }}
